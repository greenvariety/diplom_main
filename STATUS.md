# Статус

## Сессия 2026-05-28 - Правки интерфейса (HTML.md)

29. **HTML.md задача 2: действие "Перевёл студента" в журнале изменений** - при переводе студента в журнале теперь отображается "Перевёл студента" (синий бейдж) вместо "Изменил". Добавлено новое действие `transferred` в ACTION_MAP, оба вызова log_action в api_students.py обновлены, в фильтре аудита студента добавлена опция "Перевод".

28. **HTML.md задача 1: убрал баннер при переводе студента** - блок `banner banner-info` со значком «i» в модалке перевода студента заменён на обычный серый текст того же содержания.

27. **HTML.md задачи 2-4: сортировка во всех вложенных таблицах** - добавлена кликабельная сортировка по заголовкам во всех таблицах внутри карточек детальных страниц: "Классное руководство" (группа, факультет, студентов), "Ведёт предметы" (предмет, группа), "Документы" (название, тип, дата) у сотрудника; "Студенты" (ФИО, статус) у группы; "Группы" (название, год набора, студентов, кл. руководитель) у факультета; "Привязанные студенты" (студент, связь, группа) у опекуна; "Группы" (группа, преподаватель) и "Преподаватели" (преподаватель) у предмета. Хуки `useSortable` для сотрудника уже были объявлены, но не применялись - теперь применены.

26. **HTML.md задача 4/1: фильтрация групп предмета по преподавателю** - при переходе к предмету из карточки "Ведёт предметы" на странице преподавателя, страница предмета показывает только группы этого преподавателя (не всех). Карточка "Преподаватели" в режиме фильтрации скрыта, кнопки редактирования убраны, строки таблицы стали кликабельными и ведут на группу.

25. **HTML.md задача 3: добавлена кнопка "+" в карточку "Преподаватели" на странице предмета** - кнопка открывает модал назначения предмета группе (assignSubject), точно так же как кнопка в карточке "Группы".

24. **HTML.md задача 2: удалён блок "Студенты" со страницы предмета** - карточка со списком студентов (Студенты: N) полностью убрана из SubjectDetail. Студенты видны через группы, отдельный блок был лишним.


23. **HTML.md задача 1: шеврон ">" добавлен во все таблицы с кликабельными строками** - добавлен столбец-шеврон (стрелка вправо) в таблицы: список студентов, список сотрудников, список предметов, список пользователей, группы на странице факультета, группы в блоке "Классный руководитель" у сотрудника, преподаватели и студенты на странице предмета. Таблицы без шеврона оставлены: аудит-лог (открывает модал), заявки на удаление (кнопки действий), таблицы без навигации по строке.

22. **HTML.md задача 5: убрано отображение названия предмета в модале "Назначить предмет"** - когда модал открывается со страницы конкретного предмета, поле с его названием больше не показывается (и так очевидно, куда назначаем). Поле выбора предмета по-прежнему появляется, если предмет не задан заранее.

21. **Убраны кнопки добавления из секций "Студенты" и "Преподаватели" на странице предмета** - в SubjectDetail кнопка "+" удалена из заголовка карточки "Студенты" (студенты попадают автоматически через группы) и из карточки "Преподаватели" (преподаватели также назначаются через группы, отдельная кнопка была лишней).

20. **Убран текст со всех кнопок с иконкой "+" (btn-sm)** - все маленькие кнопки добавления по всему приложению теперь показывают только иконку "+" без надписей: "Добавить", "Привязать студента", "Добавить группу", "Добавить организацию" и т.д. Кнопки "Быстрые действия" (высота 44) оставлены с текстом.

19. **Имя студента в таблице "Привязанные студенты" (страница родителя) — убрана ссылка** - имя студента теперь отображается как обычный текст, без ссылки-перехода.

18. **Кнопка "Назначить" в секции "Ведёт предметы" (страница преподавателя) — убран текст** - кнопка теперь показывает только иконку "+", без надписи "Назначить".

17. **Кнопка "+" в секции "Преподаватели" SubjectDetail показывается всем ролям** - убрано ограничение owner/admin, кнопка назначения преподавателя к предмету теперь видна всем пользователям (как и кнопка в секции "Группы").

16. **Кнопка "+" в секции "Группы" SubjectDetail показывается всем ролям** - убрано ограничение owner/admin, кнопка добавления группы к предмету теперь видна всем пользователям.

15. **Добавлена кнопка "+" к секции "Студенты" в деталях предмета (SubjectDetail)** - на странице предмета в карточке "Студенты" теперь есть кнопка-плюс для добавления студента (открывает форму создания студента). Кнопка видна только для ролей owner и admin. До этого кнопки там не было совсем.


14. **Разделитель "· N" заменён на ": N" в заголовках карточек** - во всех card-head блоках, где после названия секции шёл счётчик через `· N` (точка-разделитель), теперь стоит `: N` (двоеточие). Изменено в 6 местах: "Последние действия", "Студенты", "Предметы", "Группы" (в FacultyDetail и SubjectDetail), "Преподаватели". Разделители `·` в строках-описаниях ("ФИТ · 28 студентов") оставлены без изменений.

13. **Убран подзаголовок "Групп: N" на странице предмета** - в карточке предмета (SubjectDetail) в заголовке страницы отображался подзаголовок "Групп: 1" (счётчик групп). Эта строка удалена из пропса `sub` в компоненте PageHead.

12. **Кнопка "Добавить" в списке групп** - в списке групп кнопка в шапке страницы называлась "Создать группу" вместо "Добавить". Переименована в "Добавить" - теперь все списочные экраны (студенты, сотрудники, группы, факультеты, предметы, должности, опекуны) имеют одинаковую кнопку "Добавить" в правом верхнем углу.

11. **Выравнивание блоков по горизонтали в гридах** - блоки в двух- и трёхколоночных гридах теперь выровнены по верхнему краю. Причина проблемы: глобальное CSS-правило `.card + .card { margin-top: 16px }` срабатывало и внутри гридов, сдвигая второй блок вниз на 16px. Правило заменено на `.card-stack > .card + .card { margin-top: 16px }` - теперь отступ работает только внутри контейнера с классом `card-stack`. Класс `card-stack` добавлен в правую колонку StudentDetail (там 3 карточки стакаются вертикально). Исправлено на всех экранах: дашборд, DashboardTeacher, FacultyDetail, SubjectDetail, ParentDetail.

10. **Кликабельные переходы в GroupDetail** - три элемента в карточке группы стали кликабельными: (1) имя классного руководителя в баннере - ведёт на страницу сотрудника; (2) имя студента в таблице - ведёт на страницу студента; (3) название предмета в списке предметов - ведёт на раздел "Предметы", имя преподавателя под предметом - ведёт на страницу этого сотрудника. Все переходы через `onNavigate`.

9. **Автоподстановка факультета при добавлении группы со страницы факультета** - если открываешь "Добавить группу" из FacultyDetail, факультет подставляется автоматически и поле выбора факультета не показывается. Из раздела "Группы" поле выбора факультета остается как было.

8. **Убраны хлебные крошки (breadcrumbs) со всех экранов** - строка "Факультеты / ИП" и аналогичные на других вкладках больше не отображаются. Компонент Crumbs остался в коде, но перестал рендериться в PageHead.

7. **Убрано оформление бейджа у короткого имени факультета** - в таблице списка факультетов короткое название (`short_name`) раньше отображалось в бейдже с фоном и рамкой. Теперь это просто моноширинный текст без фона и рамки.

6. **Аватарки добавлены в списки сотрудников и опекунов** - в таблице списка сотрудников и в таблице списка опекунов добавлена колонка с маленькой аватаркой (`avatar-sm`) между номером строки и ФИО - точно как у студентов. Аватарка также добавлена в таблицу опекунов на карточке студента.

5. **Бейдж с коротким названием факультета стал скруглённым квадратом** - в таблице факультетов на `<span class="badge badge-neutral mono">` добавлен inline-стиль `borderRadius: 'var(--radius-sm)'`, убрана пилюля (было `border-radius: 999px` из базового класса `.badge`).

4. **Бейдж "Сводка по системе" стал скруглённым квадратом** - у `.page-head .sub` заменён `border-radius: 20px` (пилюля) на `border-radius: var(--radius-sm)` (6px, как у кнопок).

3. **Тело модалки факультета переделано с компактный формат** - убран `dl.kv` (грид 140px + 1fr с огромным отступом), заменён на `div` с inline-форматом "Код: ИТП", "Групп: 3", "Студентов: 12" - метка серая, значение моно, отступ 6px между строками.
2. **Код факультета перенесён из подзаголовка модалки в тело** - в модалке факультета убран `sub` из заголовка, строка "Код: ИТП" теперь отображается как первая строка `dl.kv` в теле модалки.

1. **Добавлены блоки "Предметов" и "Опекунов" на дашборд** - в `DashboardOwner` и `DashboardAdmin` добавлены два новых кликабельных стат-блока: "Предметов" (иконка книги, ведёт на `/subjects`) и "Опекунов" (иконка сердца, ведёт на `/parents`). API дашборда (`api_main.py`) обновлён: теперь возвращает счётчики `subjects` и `parents`.

---

## Сессия 2026-05-27 (9) - Правки интерфейса (HTML.md)

1. **Убраны хлебные крошки "Главная / ..." со всех списочных экранов** - удалён пропс `crumbs` из `PageHead` на 6 экранах: Студенты, Сотрудники, Пользователи системы, Опекуны и родители, Предметы, Должности. Теперь эти экраны выглядят как Факультеты и Группы - без строки навигации над заголовком.

---

## Сессия 2026-05-27 (8) - Правки интерфейса (HTML.md)

1. **Кнопка "Сбросить" обведена и сделана серой** - в блоке фильтров (`.filters`) добавлено CSS-правило `.filters .btn-ghost { border-color: var(--border); color: var(--text-muted); }` в `styles.css`. Теперь кнопка "Сбросить" на всех экранах имеет тонкую рамку и серый цвет, выглядит одинаково с кнопкой "Найти" по высоте (36px). Задача 3 (sub-текст на заявках) была уже выполнена ранее коммитом, зачёркнута.

---

## Сессия 2026-05-27 (7) - Правки интерфейса (HTML.md)

1. **Убраны `!` флаги везде** - удалены все предупредительные знаки `!` (жёлтый) из списков: у студентов (нет опекунов), у сотрудников (нет должности), у групп (нет классного руководителя). Из JSX убраны три блока с условным рендером `{s.parent_count === 0 && ...}`, `{e.warn_incomplete && ...}`, `{g.warn_incomplete && ...}`.

---

## Сессия 2026-05-27 (6) - Правки интерфейса (HTML.md)

1. **Сортировка и нумерация строк во всех таблицах** - на всех 10 экранах-списках (Студенты, Сотрудники, Группы, Факультеты, Предметы, Должности, Опекуны, Пользователи, Заявки на удаление, Журнал изменений) добавлена колонка `№` с порядковыми номерами строк. Все текстовые и числовые колонки стали кликабельными заголовками с сортировкой: текст сортируется по алфавиту (рус.), числа по возрастанию/убыванию. Стрелки показывают текущее направление. Выбранная сортировка сохраняется в `localStorage` между сессиями.

---

## Сессия 2026-05-27 (5) - Правки интерфейса (HTML.md)

1. **"Всего N" стало бейджем с жирным шрифтом** - счётчик записей (Всего N факультетов / групп / сотрудников и т.д.) в заголовке каждого экрана теперь отображается как пилюля-бейдж: зелёный фон (`--accent-soft`), тёмно-зелёный текст (`--accent-ink`), жирный шрифт 700, скруглённые края. Изменён один CSS-класс `.page-head .sub` в `styles.css`.

---

## Сессия 2026-05-27 (4) - Правки интерфейса (HTML.md)

1. **Кнопка экспорта стала основного цвета** - "Настроить экспорт в Excel" на дашборде теперь `btn-primary` (основной тил-цвет проекта) вместо `btn-success` (Bootstrap зелёный).
2. **Контентная область стала компактнее** - боковые отступы `.main` уменьшены с 32px до 20px, максимальная ширина `.main-inner` уменьшена с 1280px до 1100px — содержимое выглядит плотнее, без пустого пространства по краям.
3. **Сайдбар стал шире** - ширина боковой панели увеличена с 240px до 280px (`grid-template-columns` в `.app-shell`).

---

## Сессия 2026-05-27 (2) - Правки интерфейса + наполнение БД

1. **Убраны флаги и ? кнопки** - на всех экранах (студенты, сотрудники, группы, факультеты, опекуны) убраны кнопки-флаги (треугольник предупреждения) и кнопки вопроса `?`. Убраны состояния `sortFlagged`, функции `handleToggleFlag*`.
2. **Убраны select-фильтры** - на всех списковых экранах убраны выпадающие списки фильтрации (факультет, группа, статус, должность). Теперь только текстовый поиск.
3. **Кнопки Найти/Сбросить одного размера с поиском** - кнопки стали `height: 36px` (как у поля ввода), убрана `btn-sm`.
4. **Убран банер на странице заявок** - убрана информационная надпись "Администратор подаёт заявку..." со страницы заявок на удаление.
5. **Статы на дашборде кликабельные** - Факультетов/Групп/Студентов/Сотрудников на дашборде теперь кликабельны, ведут на соответствующие экраны.
6. **Наполнена БД для greenvariety** - организация "Поволжский колледж технологий и сервиса" с 3 факультетами, 7 группами, 10 сотрудниками (с фото), 25 студентами (с фото), 10 опекунами (с фото), 15 предметами, назначениями предметов. Доп. пользователи: `admin_pcts`, `lebedev_dm`, `morozova_el` (пароль `demo_1234`).

---

## Сессия 2026-05-27 - Правки интерфейса (HTML.md)

1. **Кнопка экспорта стала зелёной** - "Настроить экспорт в Excel" на дашборде теперь `btn-success` (зелёный) вместо серого `btn-secondary`.
2. **Поиск добавлен на все экраны** - Поле поиска добавлено там где его не было: Факультеты (поиск по коду и названию), Предметы (поиск по названию), Пользователи (поиск по ФИО и логину). В Группах убран запрещённый `placeholder` и добавлена кнопка "Сбросить". У всех добавленных фильтров кнопка "Сбросить".

---

## Сессия 2026-05-26 - Правки интерфейса (HTML.md)

1. **Удалён журнал изменений с дашборда** - карточка "Журнал изменений" полностью убрана с дашборда владельца (DashboardOwner).
2. **Кнопка "Добавить" убрана, "Экспорт в Excel" переименован** - на дашборде владельца убрана кнопка "+ Добавить", кнопка "Экспорт в Excel" переименована в "Настроить экспорт в Excel".
3. **Логотип в боковой панели** - вместо буквы "У" теперь показывается изображение логотипа (`logo.png`).

---

## Сессия 2026-05-25 (6) - Задачи 5 и 6: аккаунт сотрудника + перевод между орг + история с фильтром

### Задача 5 - Блок "Аккаунт" в карточке сотрудника

В карточке сотрудника появился новый блок **"Аккаунт"** в левой колонке (видно только владельцу):
- Если у сотрудника **нет аккаунта**: показывает "Нет аккаунта" + кнопку **"Создать аккаунт"**, которая раскрывает форму с полями Логин, Пароль, Роль (Преподаватель/Администратор)
- Если аккаунт **есть**: показывает редактируемые поля Логин, Новый пароль (пустое = не менять), Роль + кнопку **"Сохранить"**
- Бэкенд: новый эндпоинт `GET/POST/PATCH /api/employees/<pk>/account/`

### Задача 6 - Перевод между организациями + улучшенная история

**Перевод студента в другую организацию:**
- В окне перевода студента (только для владельца) появился чекбокс **"Перевод в другую организацию"**
- При отметке: показываются поля Организация → Факультет → Группа (каскадная загрузка)
- Бэкенд: новый эндпоинт `POST /api/students/<pk>/transfer-institution/`
- Фасады: `/api/faculties/` и `/api/groups/` теперь принимают `?institution_id=<id>` (только для владельца)

**История изменений в карточках:**
- В карточках **студента** и **сотрудника** теперь полноценный журнал изменений (только для admin/owner)
- Фильтр по действию: Все / Создание / Изменение / Удаление
- Таблица с датой, пользователем, действием и ссылкой "Показать" (открывает модал с diff)
- Пагинация при большом числе записей
- Бэкенд: `/api/audit-log/` теперь принимает `?object_type=X&object_id=Y` для фильтрации по конкретному объекту

---

## Сессия 2026-05-25 (4) - Журнал изменений: вид создания без +/-

Изменён способ отображения записей журнала при действии "Создал":
- Вместо строк "- было пусто" и "+ новое значение" теперь просто показывается поле и его значение без плюсов и минусов.
- Заголовок секции при создании: "Поля записи" вместо "Изменённые поля - N".
- Все названия полей переведены на русский: добавлены переводы для `year` (Год начала), `full_name` (ФИО), `code` (Код), `birth_date` (Дата рождения), `short_name` (Аббревиатура), `username` (Логин), `relation_type` (Тип связи), `headteacher` (Классный руководитель), `employee` (Сотрудник), `student` (Студент), `parent` (Опекун), `subject` (Предмет), `description` (Описание), `founded_date` (Дата основания), `group_number` (Номер группы).

---

## Сессия 2026-05-25 (3) - Журнал изменений: переход к объекту

В модальном окне записи журнала добавлена кнопка **"Перейти к записи"**:
- Кнопка появляется для объектов, у которых есть детальная страница: студент, сотрудник, группа, опекун, факультет.
- Если запись содержит действие "Удалил" - кнопка показана, но заблокирована; при наведении показывается подсказка "Объект был удалён".
- Если запись содержит действие "Создал" или "Изменил" - кнопка активна, по нажатию модал закрывается и открывается страница нужного объекта.
- API (`api_audit.py`) теперь возвращает `object_id` и `object_type_raw` (тип на английском: `Student`, `Employee` и т.д.) для построения маршрута навигации.

---

## Сессия 2026-05-25 (2) - Журнал изменений: подзаголовок с лейблами

Подзаголовок в модале просмотра записи журнала изменён: теперь вместо `admin - 25.05.2026` показывается `Кто: admin  Когда: 25.05.2026` - явные лейблы с двоеточием.

---

## Сессия 2026-05-25 - Журнал изменений: что произошло с записью

Переделан модал детального просмотра записи журнала (кнопка "Показать →" в таблице).

**Что было непонятно:**
- Строка "Кто" показывала логин актора + его `display_name` — было неясно, чьё это ФИО: того кто сделал, или того кого изменили.

**Что изменилось:**
- "Кто:" - логин актора + его роль (Администратор / Преподаватель) + должность (если актор привязан к сотруднику с должностью).
- Добавлена строка "Запись о:" - показывает тип объекта (Студент / Сотрудник / Группа и т.д.) и ФИО/название из данных записи. Теперь сразу видно, чья запись изменена.
- "Когда:" и "Действие:" с двоеточием.
- Заголовок блока изменённых полей использует дефис вместо `·`.

**Бэкенд (api_audit.py):**
- `_serialize` теперь отдаёт `obj_type` (тип сущности), `obj_name` (ФИО/название из данных), `userPosition` (должность актора если есть).
- `select_related` расширен: `user__employee__position` — без N+1 запросов.

---

## Сессия 2026-05-24 (3) - Индикаторы на записях: мусорка, восклицательный, вопрос

### Что добавлено

**Знак мусорки (`I.trash` красный)** рядом с именем записи — означает, что по ней есть активная заявка на удаление. Видно в списках студентов, сотрудников и групп.

**Восклицательный знак `!` (жёлтый)** — автоматически появляется если:
- У студента нет ни одного опекуна (было реализовано ранее, сохранено)
- У сотрудника не указана должность
- У группы нет классного руководителя

**Знак вопроса `?` (синяя кнопка)** — новая функция:
- Кнопка `?` появляется в строке у каждой записи (только для admin и owner, не для teacher)
- При нажатии открывается модальное окно `NoteModal`
- Если вопроса нет — форма для ввода вопроса (до 2000 символов)
- Если вопрос уже есть — показывается текст вопроса + кто/когда написал + кнопка "Закрыть вопрос" (только для owner) + кнопка "Удалить"
- Только один активный вопрос на запись в один момент
- Синяя `?` иконка рядом с именем записи показывается если вопрос открыт

### Бэкенд

- Новая модель `RecordNote` (миграция 0016_recordnote.py)
- Новый API: `GET/POST /api/notes/`, `POST /api/notes/<id>/resolve/`, `DELETE /api/notes/<id>/`
- В данных студентов/сотрудников/групп добавлены поля: `has_pending_delreq`, `has_note`, `warn_incomplete`

---

## Сессия 2026-05-24 (2) - Ограничение символов в полях ввода

- В поля ФИО (фамилия, имя, отчество) у студентов, сотрудников и опекунов добавлено ограничение: можно вводить только русские буквы, пробел и дефис. Латинские буквы, цифры и другие символы при вводе с клавиатуры и при вставке из буфера обмена автоматически отсекаются.
- В поле "ФИО" при создании пользователя - то же ограничение (только кириллица).
- В поле "Логин" при создании пользователя - ограничение наоборот: только латиница, цифры, точка, дефис, подчёркивание.
- В поле "Полное название" факультета - ограничение: только кириллица (латинские буквы заблокированы, потому что autoCode генерирует сокращение из русских слов).
- Организация уже имела такое ограничение - не трогал.

---

## Сессия 2026-05-24 - Форма создания пользователя

- Исправлен порядок полей: "Роль" теперь занимает полную строку, поэтому "Пароль" и "Повторите пароль" отображаются рядом на одной строке (раньше были в разных строках).
- При выборе сотрудника из выпадающего списка автоматически заполняются: поле ФИО (имя сотрудника) и поле Логин (транслитерация на латинице в формате "п.петров" — первая буква имени + фамилия). Оба поля остаются редактируемыми.
- Добавлена валидация: ФИО обязательно к заполнению при создании нового пользователя.
- Глобальная уникальность логина уже проверялась на сервере (по всей базе, не только в рамках одной организации).

---

## Сессия 2026-05-23 (5) - Дизайн писем

- Убрана логика загрузки логотипа-картинки из письма (функция `_get_logo_base64`, зависимости `base64`, `io`, `os`).
- Теперь в хедере письма всегда отображается текстовый кружок "АК" — без попытки загрузить `logo.png`.
- Визуально письма стали предсказуемыми: один и тот же вид на всех почтовых клиентах, независимо от наличия файла на сервере.

---

## Сессия 2026-05-23 (4) - Глобальный автотест

Написан скрипт `tests/autotest_global.py` — комплексный авто-тест всей системы. Результат: **83/83 PASS**.

Что покрывает:
- Авторизация: вход, 401 без токена, 401 при неверном пароле, /me/, /dashboard/
- Организации: создание, список, переключение, обновление
- Должности: CRUD
- Факультеты: создание, список, обновление (у FacultyDetailView нет GET — только PATCH)
- Предметы: создание, список, обновление
- Сотрудники: CRUD, детальная страница
- Группы: создание, список, детали, назначение предмета+преподавателя
- Студенты: CRUD, смена статуса, перевод в другую группу, поиск, фильтр
- Опекуны: CRUD, привязка к студенту
- Документы: загрузка файла, GET, DELETE
- Пользователи: создание admin и teacher, список, PATCH, смена пароля, DELETE
- Права доступа: 401 без токена, 403 для teacher (создание), admin может позиции/audit-log/delete-requests
- Заявки на удаление: admin создаёт, owner видит счётчик >= 1, owner отклоняет
- Журнал изменений: GET список (26+ записей), GET users

Тестовый пользователь `autotest_owner` создаётся и удаляется автоматически через Django shell. Данные полностью очищаются после теста.

Находки при написании (не баги, а особенности реализации):
- `birth_date` и `created_at` нельзя передавать строками через API — Django хранит строку в памяти после `create()`, а `_employee_data`/`_faculty_data` вызывают `.isoformat()`/`.strftime()` на строке → 500. Фронт обходит это, не заполняя даты при создании.
- Прямого DELETE для студентов, групп, сотрудников, факультетов нет — только через delete-request approve.
- Admin может: читать audit-log, создавать должности, читать/создавать delete-requests.
- FacultyDetailView, GroupSubjectsView, StudentParentsView не имеют GET-метода.

---


## Сессия 2026-05-23 (3) - Фото студентов, опекунов, сотрудников

- В формах создания/редактирования студента, сотрудника и опекуна появился блок загрузки фото: квадрат для клика/перетаскивания изображения, лимит 5 МБ и 2000x2000 px.
- При редактировании существующей записи уже загруженное фото показывается в превью — его можно заменить или удалить.
- API для студентов, сотрудников и опекунов теперь возвращает поле `photo` с URL (или null).
- На детальных страницах студента, сотрудника и опекуна вместо инициалов показывается настоящая фотография (если загружена). Если фото нет — по-прежнему показываются инициалы.
- Фото при создании опекуна прямо из карточки студента тоже сохраняется.

---

## Сессия 2026-05-23 (2) - Порядок полей при создании факультета

- В форме "Новый факультет" поля поменяны местами: теперь сначала "Полное название", потом "Код (аббревиатура)".
- Код теперь формируется автоматически: берутся первые буквы каждого русского слова из названия (как в форме создания организации). Например, "Факультет информационных технологий" -> "ФИТ".
- Пока пользователь не трогает поле кода вручную, оно обновляется автоматически при изменении названия.
- Как только пользователь редактирует код сам - авто-генерация отключается и под полем исчезает подсказка "Формируется автоматически по названию".
- При редактировании существующего факультета авто-генерация отключена изначально (чтобы не перезатереть уже заданный код).

---

## Сессия 2026-05-23 - Валидация года создания группы

- В API создания и редактирования группы добавлена проверка: если у организации задана дата основания (`founded_date`), год начала группы не может быть раньше года основания.
- При нарушении возвращается ошибка 400 с текстом "Год начала не может быть раньше года основания организации (ГГГГ)".
- Проверка работает и при создании (POST `/api/groups/`), и при редактировании (PATCH `/api/groups/<id>/`).
- Фронтенд уже обрабатывал серверные ошибки — никаких изменений на фронте не потребовалось.

---

## Сессия 2026-05-22 (2) - Бан по email при исчерпании кодов

### Блокировка по email (не по логину)

- Лимит кодов теперь считается по адресу **email**, а не по логину. Нельзя обойти блокировку, перезаполнив форму регистрации с тем же email но другим логином.
- Функция `_register_rate_check` принимает email вместо логина, фильтрует по `email=email`.

### Бан при 5-й неверной попытке на 5-м коде

- Если пользователь запросил **5 кодов** (максимум) и на последнем коде ввёл неверный код **5 раз**, то на 5-й неверной попытке получает ответ 429 с сообщением "Запросы истекли. Повторите через 15 минут" вместо "Запросите новый код" (нового кода всё равно не будет).
- То же самое, если 5 попыток уже исчерпаны и пользователь снова заходит на экран ввода последнего кода.

### Фронтенд

- При получении 429 в обработчике `submit` (ввод кода) - кнопка "Отправить снова" блокируется (isBlocked = true), точно так же как при 429 при повторной отправке.

---

## Сессия 2026-05-22 - Ограничение попыток кода + проверка занятости логина/email

### Ограничение попыток ввода кода при регистрации

- Теперь на каждый присланный код дается ровно **5 попыток** ввода. Если все 5 неверные - код "умирает" и под полем ввода появляется сообщение "Превышено число попыток. Запросите новый код".
- Всего можно запросить **5 кодов** (первый + 4 повторных). Если все 5 кодов исчерпаны, при попытке запросить новый - под полем ввода появляется "Запросы истекли. Повторите через 15 минут", кнопка "Отправить снова" блокируется.
- Окно блокировки - **15 минут** (было 60 минут).

### Проверка занятости логина и email при регистрации

- При потере фокуса с поля **Логин** - система проверяет, занят ли логин, и сразу под полем показывает ошибку "Логин уже занят. Выберите другой".
- При потере фокуса с поля **Email** - аналогичная проверка: "Этот email уже зарегистрирован".
- Проверка срабатывает только если введенное значение прошло локальную валидацию (формат правильный). Если пользователь меняет значение - ошибка сбрасывается.
- При сабмите формы ошибки занятости логина/email тоже отображаются под полями (а не только во всплывашке).
- Добавлена проверка уникальности email на бэкенде в RegisterView.
