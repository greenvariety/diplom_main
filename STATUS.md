# Статус

## Сессия 2026-05-19 - Пользовательское соглашение при регистрации

На форме создания аккаунта (шаг 1) добавлен чекбокс "Я ознакомился(-лась) и согласен(-на) с пользовательским соглашением".

Что сделано:
- Без галочки кнопка "Далее" не срабатывает - выводится ошибка и тост
- Рядом с текстом ссылка "пользовательским соглашением" - по клику открывается отдельная страница
- Страница соглашения оформлена в том же стиле что и сайт (карточка, те же шрифты и цвета)
- 8 разделов: предмет соглашения, пользователи, персональные данные, обязанности, права администрации, ограничение ответственности, срок действия, применимое право
- После прочтения кнопка "Вернуться к регистрации" возвращает на форму (галочка сохраняется если была поставлена до перехода)

---

## Сессия 2026-05-19 - 6 ячеек для ввода кода

Поле ввода кода заменено на 6 отдельных ячеек (по одному символу в каждой). Применено на экране подтверждения email при регистрации и на экране восстановления пароля.

Что работает:
- Печатаешь символ - фокус автоматически переходит в следующую ячейку
- Backspace - очищает текущую ячейку или переходит к предыдущей
- Стрелки влево/вправо - навигация между ячейками
- Можно вставить код из буфера через Ctrl+V - заполняет ячейки начиная с текущей
- Разрешены только буквы A-Z и цифры 0-9, дефис и любые другие символы просто игнорируются при вводе
- После "Отправить снова" - все ячейки очищаются, фокус на первой
- Все символы автоматически переводятся в верхний регистр

---

## Сессия 2026-05-19 - Защита кода верификации + таймер повторной отправки

**Что изменилось в базе данных:**
- Добавлено поле `attempts` (число неверных попыток) в таблицу `EmailCode`
- Миграция `0013_emailcode_attempts` применена

**Правила проверки кода (бэкенд):**
- Код теперь живёт 10 минут (было 15)
- Повторно запросить код можно только через 60 секунд - если раньше, вернётся ошибка с полем `retry_after` (сколько секунд ждать)
- Если за последний час для этого логина было отправлено 5+ кодов - новый код не выдаётся (ошибка "Слишком много попыток. Попробуйте позже"), без объяснения причины
- Если пользователь 20 раз ввёл неверный код - даже правильный код больше не принимается, нужно запросить новый

**Новый API-эндпоинт:**
- `POST /api/auth/resend-register-code/` - повторная отправка кода при регистрации (с учётом всех лимитов)

**Что изменилось на фронтенде (экран ввода кода):**
- Текст "Код действителен 15 минут" исправлен на "10 минут"
- Вместо ссылки "Вернуться и отправить снова" - кнопка "Отправить снова"
- Кнопка неактивна 60 секунд, показывает обратный отсчёт "Отправить снова (59 сек.)"
- После нажатия: отправляет новый код, сбрасывает таймер на 60 секунд и очищает поле кода
- Если сервер вернул `retry_after` - таймер устанавливается на это значение

---

## Сессия 2026-05-19 - Реальная отправка писем через Gmail

Починена отправка кодов подтверждения на почту. Раньше они выводились только в консоль сервера и никуда не шли.

Теперь Django отправляет реальные письма через Gmail SMTP:
- Создан `.env` в корне проекта с логином и паролем приложения Gmail (не попадает в git - уже был в .gitignore)
- `settings.py` читает `.env` при запуске и автоматически включает SMTP
- Если `.env` нет - письма снова идут в консоль, сервер не падает
- Если письмо не ушло - пользователь видит сообщение об ошибке, код удаляется из базы

Добавлено правило в `CLAUDE.md`: все пароли и секретные ключи хранить только в `.env`.
Добавлен `.env.example` - шаблон с именами переменных без значений.

---

## Сессия 2026-05-18 - Email-верификация вместо сид-фразы

**Что удалено:**
- Модель `SeedPhrase` и вся связанная логика (словарь BIP39, хэширование)
- Экран показа сид-фразы после регистрации
- Поля 12 слов из модала удаления организации и восстановления пароля

**Регистрация:**
- Пользователь вводит логин, ФИО, email, пароль
- На email приходит 6-значный код (буквы A-Z + цифры 0-9)
- Второй шаг: ввод кода - создаётся аккаунт, выполняется вход

**Удаление организации:**
- Нажать кнопку удаления - кнопка "Отправить код на почту"
- На email владельца приходит код
- Второй шаг: ввести код - организация удаляется

**Восстановление пароля:**
- Шаг 1: ввести логин, нажать "Получить код" - на email приходит код
- Шаг 2: ввести код + новый пароль

**Техническая часть:**
- Новая модель `EmailCode` в БД (миграция 0012)
- Поле `email` добавлено в модель `User`
- Email-бэкенд: выводит письма в консоль Django сервера (для разработки)
- Новые API: `/api/auth/verify-email/`, `/api/auth/recover/send-code/`, `/api/organizations/<id>/send-delete-code/`
- Все 30 тестов прошли успешно
